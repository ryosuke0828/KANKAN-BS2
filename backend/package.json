{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for KANKAN-BS2",
  "main": "dist/presentation/server.js",
  "scripts": {
    "clean": "rm -rf dist deployment_package.zip",
    "build": "tsc",
    "package": "npm run clean && docker build -t kankan-bs2-backend-builder . && docker run --rm -v $(pwd):/host kankan-bs2-backend-builder cp /tmp/deployment_package.zip /host/deployment_package.zip",
    "dev": "ts-node -r tsconfig-paths/register presentation/server.ts",
    "package": "npm run clean && docker build -t kankan-bs2-backend-builder . && docker run --rm -v $(pwd):/host kankan-bs2-backend-builder cp /tmp/deployment_package.zip /host/deployment_package.zip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/express": "^5.0.5",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.10.0",
    "@types/supertest": "^6.0.3",
    "@types/uuid": "^8.3.4",
    "esbuild": "^0.23.0",
    "supertest": "^7.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.925.0",
    "@aws-sdk/client-kms": "^3.929.0",
    "@aws-sdk/lib-dynamodb": "^3.925.0",
    "@slack/web-api": "^7.12.0",
    "bcrypt": "^6.0.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "serverless-http": "^4.0.0",
    "uuid": "^8.3.2"
  }
}

